package com.reto.tecnico.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.reto.tecnico.entities.ClaseNave;

public interface ClaseNaveRepository extends JpaRepository<ClaseNave, Long>{ 
	/* Estructura de la interfaz para la clase ClaseNave */

}
